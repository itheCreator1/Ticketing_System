<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <!-- Bootstrap Icons -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
  <!-- Tailwind CSS -->
  <link rel="stylesheet" href="/css/output.css">
</head>
<body class="bg-gray-50">
  <%- include('../../partials/header') %>

  <main class="container-fluid px-4 py-6">
    <h2 class="text-2xl font-bold mb-4">Create New Department</h2>
    <%- include('../../partials/flash') %>

    <div class="card" style="max-width: 600px;">
      <div class="card-body p-4">
        <form action="/admin/departments" method="POST">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>">

          <div class="mb-3">
            <label for="name" class="form-label">Department Name *</label>
            <input type="text" id="name" name="name" class="form-input" required maxlength="100">
            <small class="text-sm text-gray-600">Unique name for the department (e.g., "Marketing", "Legal")</small>
          </div>

          <div class="mb-4">
            <label for="description" class="form-label">Description</label>
            <textarea id="description" name="description" class="form-textarea" rows="3" maxlength="500"></textarea>
            <small class="text-sm text-gray-600">Optional description of the department's role</small>
          </div>

          <div class="flex gap-2">
            <button type="submit" class="btn btn-primary">Create Department</button>
            <a href="/admin/departments" class="btn btn-secondary">Cancel</a>
          </div>
        </form>
      </div>
    </div>
  </main>

  <%- include('../../partials/footer') %>
</body>
</html>
